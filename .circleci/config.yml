version: 2.1

executors:
  python:
    docker:
    - image: circleci/python:3.8

jobs:
  build-and-test:
    executor: python
    steps:
      - checkout
      - run:
          command: python3 test.py
          name: Test

workflows:
  main:
    jobs:
      - build-and-test
